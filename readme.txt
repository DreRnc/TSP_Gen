To run the whole code for the three different cities, simply run "source sh/run.sh".
This will run sequential, static, dynamic and ff solutions, for all of the three cities.
To generate plots, run 
> poetry install
> poetry shell 
> python utils/plotting.py